# URL of the A2A agent server. Defaults to http://localhost:4000 if unset.
VITE_AGENT_URL=http://localhost:4000
# URL of the file-storage service (attachment uploads). Defaults to http://localhost:6060 if unset.
VITE_FILE_STORAGE_URL=http://localhost:6060
